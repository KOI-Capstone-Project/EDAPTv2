# ── Web framework ─────────────────────────────────────────────────────────
fastapi==0.115.0
uvicorn[standard]==0.30.6
gunicorn==22.0.0

# ── Database ──────────────────────────────────────────────────────────────
sqlalchemy[asyncio]==2.0.36
asyncpg==0.29.0
alembic==1.13.3

# ── Data / ML ─────────────────────────────────────────────────────────────
pandas==2.2.3
scikit-learn==1.5.2
joblib==1.4.2
# numpy bumped 1.26.4 -> 2.4.6 by shap's own dependency resolution (shap
# requires numpy>=2). Verified scikit-learn/xgboost/pandas above are all
# still compatible — full test suite passes unchanged on 2.4.6.
numpy==2.4.6
xgboost==2.1.4
imbalanced-learn==0.12.4
shap==0.52.0

# ── Config & validation ───────────────────────────────────────────────────
pydantic==2.9.2
pydantic[email]==2.9.2
pydantic-settings==2.5.2
python-dotenv==1.0.1

# ── Auth — JWT + password hashing ─────────────────────────────────────────
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-multipart==0.0.9
aiofiles==23.2.1

# ── Auth — OAuth sign-in (Google ID token verification) ───────────────────
# Microsoft ID tokens are verified with python-jose above against Azure
# AD's own JWKS — no separate MSAL/server-side library needed for that.
# Version matches what google-generativeai already pulls in transitively —
# pinned explicitly here since we now import it directly too.
google-auth==2.56.3

# ── HTTP client (Gemini API calls) ────────────────────────────────────────
httpx==0.27.2

# ── Google Gemini SDK ─────────────────────────────────────────────────────
google-generativeai==0.8.3

# ── AI Config — additional provider SDKs (Settings > AI Config lets an
# admin pick Anthropic/Gemini/OpenAI at runtime instead of being locked to
# whichever one is installed/configured via env var) ──────────────────────
anthropic==0.39.0
openai==1.54.4

# ── Dev / testing ─────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
httpx==0.27.2          # used by TestClient
